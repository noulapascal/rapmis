<!DOCTYPE html>

<title>New LienUtile</title>

{% block body %}
    <h1>{% trans %}Create new LienUtile{% endtrans %}</h1>

    {{ include('lien_utile/_form.html.twig') }}

    <a href="{{ path('lien_utile_index') }}">back to list</a>
{% endblock %}
