<!DOCTYPE html>

<title>Edit LienUtile</title>

{% block body %}
    <h1>{% trans %}Edit LienUtile{% endtrans %}</h1>

    {{ include('lien_utile/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lien_utile_index') }}">back to list</a>

    {{ include('lien_utile/_delete_form.html.twig') }}
{% endblock %}
