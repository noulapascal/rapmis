{% extends 'base.html.twig' %}

{% block body %}
  <div class="page-content-wrapper">
        <div class="page-content">
            <div class="page-bar">
                <div class="page-title-breadcrumb">
                    <div class=" pull-left">

                    </div></div></div>
    {{ form_start(form) }}
    {#{ form_row(form.student) }#}
    <div class="box-footer clearfix">
     {{ form_end(form) }}   
        <button type="submit" class="mdl-button mdl-button--raised mdl-js-ripple-effect m-b-10 btn-primary pull-right">Create <i class="fa fa-paper-plane-o"></i></button>

             
             
              <div class="table-scrollable">
                                                    <table class="table table-striped table-bordered table-hover table-checkable order-column valign-middle" id="example4">
                                                        <thead>
                                                        <tr>
                                                            <th></th>
                                                            <th>{% trans %} Name {% endtrans %}
                                                            <th>{% trans %}First name {% endtrans %}  </th>
                                                            <th>{% trans %} Class {% endtrans %}</th>
                                                            <th>{% trans %} City {% endtrans %}</th>
                                                            <th>{% trans %} School{% endtrans %}</th>
                                                            <th>{% trans %} Action {% endtrans %}</th>
                                                        </tr>
                                                        </thead>
                                                        <tbody>
                                                        {% for student in rep  %}

                                                            <tr class="odd gradeX">
                                                                <td class="patient-img">
                                                                    <img src="{{ asset('uploads/students/' ~ student.filename) }}" alt="">
                                                                </td>
                                                                <td class="left">{{ student.name }}</td>
                                                                <td class="left">{{ student.firstName }}</td>
                                                                <td>{% if student.classes %}{{ student.classes }}{% else %}No{% endif %}</td>
                                                                <td class="left">{{ student.classes.etablissements.city}}</td>
                                                                <td class="left">{% if student.classes.etablissements %}{{ student.classes.etablissements }}{% endif %}</td>
                                                                <td>
                                                                     <a href="{{ path('students_show', { 'id': student.id, }) }}" class="btn  btn-info fa fa-list btn-xs">voir</a>
                                                                    {% if is_granted('ROLE_ADMIN_SEC') %}
                                                                     <a href="{{ path('students_edit', { 'id': student.id }) }}" class="btn btn-primary btn-xs">
                                                                        <i class="fa fa-pencil"></i>
                                                                    </a>
                                                                       
                                                                    <a href="{{ path('students_delete', { 'id': student.id, }) }}" >
                                                                       <button class="btn btn-danger btn-xs">
                                                                        <i class="fa fa-trash-o "></i>
                                                                    </button>
                                                                       {% endif %}
                                                                </a>
                                                                </td>
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                </div>
            
    </div>
                 
        </div></div>
       
    {# <ul>
         <li>
             <a href="{{ path('epreuves_index') }}">{% trans %}Back To The List{% endtrans %}</a>
         </li>
     </ul>#}

   <script src="{{ asset('assets/js/select.js') }}" type="text/javascript">
       
       </script>
{% endblock %}
