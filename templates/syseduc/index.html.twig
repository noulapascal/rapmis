{% extends 'base.html.twig' %}

{% block body %}
<div class="page-content-wrapper">
    <div class="page-content">
        <div class="page-bar">
            <div class="page-title-breadcrumb">
                <div class=" pull-left">
                    <div class="page-title">{% trans %} Dashboard {% endtrans %}</div>
                </div>
                <ol class="breadcrumb page-breadcrumb pull-right">
                    <li><i class="fa fa-home"></i>&nbsp;<a class="parent-item" href="{{ path('homepage') }}">{% trans %} Home {% endtrans %}</a>&nbsp;<i class="fa fa-angle-right"></i>
                    </li>
                    <li class="active">{% trans %} Dashboard {% endtrans %}</li>
                </ol>
            </div>
        </div>
    <h1>{% trans %}Syseducs list{% endtrans %}</h1>

    <table>
        <thead>
            <tr>
                <th>{% trans %} Id{% endtrans %}</th>
                <th>{% trans %} Name {% endtrans %}</th>
                <th>{% trans %}Description{% endtrans %}</th>
                <th>{% trans %}Actions{% endtrans %}</th>
            </tr>
        </thead>
        <tbody>
        {% for sysEduc in sysEducs %}
            <tr>
                <td><a href="{{ path('syseduc_show', { 'id': sysEduc.id }) }}">{{ sysEduc.id }}</a></td>
                <td>{{ sysEduc.name }}</td>
                <td>{{ sysEduc.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('syseduc_show', { 'id': sysEduc.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('syseduc_edit', { 'id': sysEduc.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('syseduc_new') }}">Create a new sysEduc</a>
        </li>
    </ul>
    </div>
</div>
{% endblock %}
